"""Single source of truth for the application version.

Bump `__version__` on each release; the GUI version label and the M7.5 update
checker both read it from here so they can never drift apart.
"""

__version__ = "1.0.0"
